<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>Book Notes</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
        <link rel="stylesheet" href="./CSS/style.css" />
    </head>

    <%- include('header.ejs') %>

    <body style="padding-top: 100px;">
        <div class="container mt-5">
            <div class="hero-section text-center mb-5 p-5 rounded" style="background: linear-gradient(135deg, #343a40 0%, #495057 100%); color: white;">
                <h1 class="display-3 fw-bold mb-3">Book Notes</h1>
                <p class="lead mb-4">Your personal digital library awaits</p>
                <div class="row justify-content-center">
                    <div class="col-auto">
                        <div class="d-flex gap-4">
                            <div class="text-center">
                                <div class="h3 fw-bold"><%= books.length %></div>
                                <small>Books</small>
                            </div>
                            <div class="text-center">
                                <div class="h3 fw-bold">∞</div>
                                <small>Memories</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="container">
                <% if (books.length > 0) { %>
                    <div class="devices">
                        <% books.forEach(book => { %>
                            <div class="device-info">
                                <div class="row mb-5">
                                    <div class="col-sm-4 col-md-3">
                                        <img src="<%= book.cover %>" alt="Book Cover" class="album-cover">
                                    </div>
                                    <div class="col-sm-8 col-md-9">
                                        <div class="text-muted mt-2 mb-1">
                                            <i class="bi bi-person me-2"></i><%= book.author %>
                                        </div>
                                        <i class="bi bi-pen"></i>
                                        <a href="/edit/<%= book.id %>" class="h5 mb-1 text-decoration-none"><%= book.title %></a>
                                        <!-- <div class="h5 mb-1"><%= book.title %></div> -->
                                        <div class="text-muted mb-0">
                                            <i class="bi bi-book me-2"></i><%= book.description %>
                                        </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <p>No books found.</p>
                <% } %>
            </div>
        </div>
        <!-- script -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>
    </body>

    <%- include('footer.ejs') %>

</html>
